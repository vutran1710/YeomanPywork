[DEVELOPMENT]
log_level = 10
<% if (redis || aioredis) { %> redis_url = "redis://user:password@localhost:6379" <% } %>

[TEST]
log_level = 10
<% if (redis || aioredis) { %> redis_url = "redis://user:password@localhost:6379" <% } %>

[PRODUCTION]
log_level =
<% if (redis || aioredis) { %> redis_url =  <% } %>